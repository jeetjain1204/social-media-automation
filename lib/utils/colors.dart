import 'package:flutter/material.dart';

const darkColor = Color.fromRGBO(0, 47, 110, 1);
const lightColor = Color.fromRGBO(186, 226, 255, 1);

// ─── Text & Backgrounds ──────────────────────────────
const backgroundColor = Color(0xFFF9FAFB); // --bg
const textColor = Color(0xFF111827); // --text (dark text for light BGs)
const whiteColor = Color(0xFFFFFFFF);
const blackColor = Color(0xFF000000);

// ─── Feedback & Status ───────────────────────────────
const successColor = Color(0xFF22C55E); // --success (green)
const warningColor = Color(0xFFF59E0B); // --warning (amber)
const errorColor = Color(0xFFEF4444); // --error (red)

// ─── Neutral Greys (for strokes, inputs, placeholders) ─────
const grey50 = Color(0xFFF9FAFB); // Lightest
const grey100 = Color(0xFFF3F4F6);
const grey200 = Color(0xFFE5E7EB);
const grey300 = Color(0xFFD1D5DB);
const grey400 = Color(0xFF9CA3AF);
const grey500 = Color(0xFF6B7280); // Placeholder text
const grey600 = Color(0xFF4B5563);
const grey700 = Color(0xFF374151);
const grey800 = Color(0xFF1F2937);
const grey900 = Color(0xFF111827); // Same as textColor

// ─── Shadows & Overlays ───────────────────────────────
const shadowColor = Color.fromRGBO(0, 0, 0, 0.06); // --shadow-card
const overlayColor = Color.fromRGBO(0, 0, 0, 0.3); // for modals/backdrops
